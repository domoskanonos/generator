package ${model.subItems[0].javaPackageName};

import lombok.*;

import javax.persistence.*;
import java.util.Date;
import java.math.BigDecimal;

#if($model.subItems[0].useJPAIdClazz)@IdClass(${model.subItems[0].javaIdClazzSimpleName}.class)#end
@Entity
@Table(name = "${model.subItems[0].tableName}")
@Data
@ToString
@Builder(toBuilder = true)
@EqualsAndHashCode
@NoArgsConstructor
@AllArgsConstructor
public class ${model.subItems[0].javaClazzSimpleName} {
#foreach( $property in $model.subItems[0].properties )
    ${property.jpaPropertyAnnotations}
    private #if(${property.propertyType.javaTypeSimpleName}=='TYPE_DATE_ISO8601')Date#else${property.propertyType.javaTypeSimpleName}#end ${property.name};
#end

}