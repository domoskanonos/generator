package ${model[0].projectDTO.javaPackageName};

import lombok.*;

import javax.persistence.*;
import java.util.Date;
import java.math.BigDecimal;

#if($model[0].useJPAIdClazz)@IdClass(${model[0].javaIdClazzSimpleName}.class)#end
@Entity
@Table(name = "${model[0].tableName}")
@Data
@ToString
@Builder(toBuilder = true)
@EqualsAndHashCode
@NoArgsConstructor
@AllArgsConstructor
public class ${model[0].javaClazzSimpleName} {
#foreach( $property in $model[0].properties )
    ${property.jpaPropertyAnnotations}
    private #if(${property.propertyType.javaTypeSimpleName}=='TYPE_DATE_ISO8601')Date#else${property.propertyType.javaTypeSimpleName}#end ${property.name};
#end

}