export class ${model.typescriptModelName} {
#foreach( $property in $model.properties )
    ${property.name} : ${property.propertyType.typescriptType} = ${property.propertyType.typescriptInitValue};
#end
}